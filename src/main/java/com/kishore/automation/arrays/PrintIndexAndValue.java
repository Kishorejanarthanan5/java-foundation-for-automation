package com.kishore.automation.arrays;

public class PrintIndexAndValue {
    public static void main(String[] args) {
        int[] numbers = {10, 20, 30};

        for (int i = 0; i < numbers.length; i++) {
            System.out.println("Index: " + i + " Value: " + numbers[i]);
        }
    }
}
